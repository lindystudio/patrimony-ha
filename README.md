# Patrimony Collection — Home Assistant

Private custom component and Supervisor add-on. Live state stays on the house. No tokens in this repo.

- `custom_components/patrimony_collection` — PresentationDocument over HA HTTP/WebSocket
- `addons/patrimony_collection` — optional mapping UI in the Add-ons grid
- Demo fixtures under `fixtures/` use a placeholder property (`Demo Home`)
